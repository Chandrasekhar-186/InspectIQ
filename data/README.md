# Data

Raw datasets are intentionally excluded from Git.

Expected datasets:

- `raw/severstal/`
- `raw/neu_det/`
- `raw/gc10_det/`

The dataset preparation pipeline will inspect licenses, annotations,
class distributions, duplicates, and split strategy before training.

Demo videos belong under `demo/relevant/` and `demo/irrelevant/`.
