[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "inspectiq"
version = "0.1.0"
description = "AI-powered steel manufacturing inspection platform"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
